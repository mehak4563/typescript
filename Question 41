function showMagicians(magicians: string[]): void {
    for (const magician of magicians) {
        console.log(magician);
    }
}

const magicianNames: string[] = ['alesha', 'amna', 'ayesh', 'mehak'];

showMagicians(magicianNames);
